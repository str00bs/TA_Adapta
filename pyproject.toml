[tool.poetry]
name = "acme"
version = "0.1.0"
description = ""
authors = ["Ada <github@adapdr.me>"]
readme = "README.md"
packages = [{include = "acme", from = "src"}]

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
